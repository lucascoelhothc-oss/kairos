      - name: Install dependencies
        run: |
          # Se existir package-lock.json (ou npm-shrinkwrap.json / yarn.lock) usa if [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ] || [ -f yarn.lock ]; then`n            npm ci --prefer-offline --no-audit --no-fund`n          else`n            npm install --no-audit --no-fund`n          fi; senão usa npm install
          if [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ] || [ -f yarn.lock ]; then
            echo "Lockfile found, running if [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ] || [ -f yarn.lock ]; then`n            npm ci --prefer-offline --no-audit --no-fund`n          else`n            npm install --no-audit --no-fund`n          fi"
            if [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ] || [ -f yarn.lock ]; then`n            npm ci --prefer-offline --no-audit --no-fund`n          else`n            npm install --no-audit --no-fund`n          fi --prefer-offline --no-audit --no-fund
          else
            echo "No lockfile found, running npm install"
            npm install --no-audit --no-fund
          fi
        shell: bash
